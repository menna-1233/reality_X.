# الأدوار والصلاحيات — RealityX

ده ملخص سريع لمين بيشوف إيه وبيدخل إزاي، عشان أي حد في الفريق يفهم بسرعة.

## المستخدم العادي (مواطن) — من غير أي تسجيل دخول

المواطن بيدخل على الفرونت إند ويستخدمه على طول، من غير حساب أو باسورد خالص.

| الصفحة | الرابط | الوظيفة |
|---|---|---|
| بلاغ جديد | `/` | يبعت صورة + وصف + موقع → الباك إند يحلل بالـ AI ويرد بالنتيجة |
| فيد البلاغات | `/feed` | يشوف كل البلاغات (بتاعته وبتاع غيره) |
| تفاصيل بلاغ | `/reports/:id` | تفاصيل بلاغ واحد + البلاغات التانية المرتبطة بنفس المشكلة |

مفيش أي حاجز تسجيل دخول قدامه على الصفحات دي.

## الإدارة (Admin) — بإيميل وباسورد

**طريقة الدخول:**
1. يفتح `/dashboard` أو `/findings` (أو يدوس "نظرة عامة"/"إدارة البلاغات" في القائمة الجانبية).
2. لو مش مسجل دخول، بيتحوّل تلقائيًا لـ `/admin/login`.
3. بيدخل الإيميل والباسورد بتاعته — حساب حقيقي في Supabase Auth، لازم يكون
   عنده `role = 'admin'` في جدول `profiles`.
4. بعد الدخول بيرجعله على نفس الصفحة اللي كان رايحلها.

**بيشوف زيادة عن المواطن:**

| الصفحة | الرابط | الوظيفة |
|---|---|---|
| نظرة عامة | `/dashboard` | خريطة + إحصائيات + نشاط لكل البلاغات |
| إدارة البلاغات | `/findings` | كانبان/جدول لكل البلاغات + **تغيير حالة أي بلاغ** |

وفي زرار "تسجيل خروج (الإدارة)" في أسفل القائمة الجانبية.

## الحماية الفعلية (مش بس شكل الواجهة)

- تغيير حالة بلاغ (`PATCH /reports/{id}`, `PATCH /incidents/{id}`) وقراءة
  الإحصائيات (`GET /stats`) — الباك إند بيرفضهم (401/403) لو مفيش توكن أدمن
  صحيح، حتى لو حد نادى الـ API مباشرة من غير الفرونت.
- كل حاجة تانية (`POST /reports`, `GET /reports`, `GET /incidents`, `/analyze`)
  مفتوحة تمامًا — زي ما هو متوقع من نظام تبليغ عام.
- التفاصيل التقنية الكاملة (الكود، الـ dependency، RLS) في
  [`backend/README.md`](backend/README.md#access-model-anonymous-citizens-admin-login).

## حساب أدمن تجريبي (للتست)

في حساب أدمن تجريبي متعمل بالفعل على مشروع Supabase (`urbaneye-ai`) للتجربة:

- **الإيميل:** `admin@urbaneye.demo`
- **الباسورد:** اتبعت في محادثة الفريق (مش متحطوطة هنا عن قصد — عشان الباسورد
  ميدخلش تاريخ الـ git بشكل دائم). اسألي زميلة الفريق اللي عملته، أو
  اعملي حساب أدمن جديد بنفسك: تسجّلي user عادي في Supabase Auth، وبعدين
  تحطي `role = 'admin'` في صف `profiles` بتاعه.
