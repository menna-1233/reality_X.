# Base URL of the UrbanEye FastAPI backend (see backend/).
VITE_API_BASE_URL=http://localhost:8000

# Supabase project — used ONLY for admin email/password login (see
# src/lib/supabaseClient.ts). Citizens never authenticate; these are the
# anon/publishable key, safe to ship to the browser.
VITE_SUPABASE_URL=https://ccvdyifhquvcdixaqpat.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_wo3XHW100Qtq4bCEIOVLBQ_aEUR64Hd
